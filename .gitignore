node_modules/
.vercel/

# the original hand-off: the practical records are the student's own documents
# and are not published with the site
Chem-Lab/
*.docx
.claude/

# .env.local holds a VERCEL_OIDC_TOKEN written by the Vercel CLI
.env*
